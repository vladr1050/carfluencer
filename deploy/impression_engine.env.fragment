# Merged by: php artisan telemetry:ensure-env (deploy/post-pull.sh). Idempotent: only missing keys appended.
# Impression Engine: Uber H3 via PHP FFI (requires libh3 on the host; Docker image includes libh3-dev).
IMPRESSION_ENGINE_H3_DRIVER=real
# Optional if libh3.so is not on default ld path:
# IMPRESSION_ENGINE_H3_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu/libh3.so
