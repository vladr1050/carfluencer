; Supervisor — очередь Laravel (ClickHouse sync и прочие jobs).
; Устанавливается скриптом setup-ubuntu-server.sh в /etc/supervisor/conf.d/carfluencer-queue.conf
; При другом корне репозитория подставь путь к artisan.

[program:carfluencer-queue]
process_name=%(program_name)s_%(process_num)02d
; Воркер очереди: лимит не ниже job (CAMPAIGN_REPORT_PHP_MEMORY_LIMIT), иначе ini_set в job не поможет.
command=/usr/bin/php8.4 -d memory_limit=1024M /var/www/carfluencer/backend/artisan queue:work database --sleep=3 --tries=3 --max-time=3600
autostart=true
autorestart=true
stopasgroup=true
killasgroup=true
user=www-data
numprocs=1
redirect_stderr=true
stdout_logfile=/var/www/carfluencer/backend/storage/logs/worker.log
stopwaitsecs=3600
