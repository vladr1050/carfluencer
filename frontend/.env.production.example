# Production API URL (same origin as SPA in browser, no trailing slash)
# Прод: https://www.carplace.lv — обычно VITE_API_URL не нужен (см. docs/DEPLOY/16_carplace_lv.md)
# Один хост с API (Nginx → Laravel): оставьте закомментированным — запросы пойдут на /api относительно https://www.carplace.lv
# VITE_API_URL=https://www.carplace.lv
